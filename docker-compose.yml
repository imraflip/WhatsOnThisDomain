services:
  wotd:
    build: .
    image: wotd:dev
    volumes:
      - ./wotd:/app/wotd
      - ./tests:/app/tests
      - wotd-config:/root/.config/wotd
      - wotd-data:/root/.local/share/wotd
    stdin_open: true
    tty: true

  dev:
    build: .
    image: wotd:dev
    entrypoint: []
    command: bash
    volumes:
      - ./wotd:/app/wotd
      - ./tests:/app/tests
      - wotd-config:/root/.config/wotd
      - wotd-data:/root/.local/share/wotd
    stdin_open: true
    tty: true

volumes:
  wotd-config:
  wotd-data:
